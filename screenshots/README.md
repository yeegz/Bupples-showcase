# Screenshots

Drop your app screenshots here so they render in the main README. Expected names
(taken on a phone / simulator, PNG):

- `home.png` — the wallet / hangouts list
- `session.png` — a session with the bubble field
- `add-expense.png` — the add-expense sheet
- `settle.png` — the settle-up flow

Tip: an animated `demo.gif` (drag a bubble, add an expense, settle up) is the
single most persuasive thing you can add — link it under "Demo walkthrough" in
the main README.
